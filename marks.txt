сломался <File name="logToFile" fileName="${logDir}/logFile_API.log">, не пишет в него данные

как правильно логгировать ошибки теста, если после ассерта тест заканчивается?

как в БД писать логи с другими параметрами в отдельные столбцы (допустим в коде хочу передавать uuid теста, который будет в каждом методе, который этим тестом вызывается. Типа поиск бизнес процесса по uuid)

не показываются в аллюре тесты BeforeAll AfterAll

